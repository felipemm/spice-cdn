# Optional: Argo CD Application for OpenCost (Kind-friendly ingress for UI).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: opencost
  namespace: argocd
spec:
  project: default
  source:
    chart: opencost
    repoURL: https://opencost.github.io/opencost-helm-chart
    targetRevision: 1.33.1
    helm:
      releaseName: opencost
      # Keep in sync with values-kind.yaml (chart 1.33.1: prometheus + ui ingress live under `opencost:`).
      values: |
        opencost:
          prometheus:
            internal:
              enabled: true
          ui:
            ingress:
              enabled: true
              ingressClassName: nginx
              hosts:
                - host: opencost.127.0.0.1.nip.io
                  paths:
                    - /
  destination:
    server: https://kubernetes.default.svc
    namespace: opencost
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
