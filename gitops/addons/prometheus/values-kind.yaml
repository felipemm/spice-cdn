# Minimal Prometheus for Kind + OpenCost (matches OpenCost 1.33.1 internal defaults).
# Service name: prometheus-server, namespace: prometheus-system, port: 80.

alertmanager:
  enabled: false

pushgateway:
  enabled: false

server:
  fullnameOverride: prometheus-server
  persistentVolume:
    enabled: false
